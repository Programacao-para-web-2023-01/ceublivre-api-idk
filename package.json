{
  "scripts": {
    "dev": "wrangler dev src/index.ts --local --persist",
    "migrate:dev": "wrangler d1 migrations apply ceublivre-api-idk --local",
    "migrate": "wrangler d1 migrations apply ceublivre-api-idk",
    "prettier": "prettier --write .",
    "test": "jest",
    "deploy": "wrangler publish src/index.ts"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.1.2",
    "hono": "^3.1.0",
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230321.0",
    "@types/jest": "^29.5.1",
    "better-sqlite3": "^8.2.0",
    "esbuild": "^0.15.1",
    "esbuild-jest": "^0.5.0",
    "jest": "^29.5.0",
    "jest-environment-miniflare": "^2.6.0",
    "prettier": "2.8.8",
    "wrangler": "^2.7.1"
  }
}
